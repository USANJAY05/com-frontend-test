// Feature boundary. The implementation remains in the legacy component path
// for now so imports and lazy loading stay backward compatible.
export { default } from '../../components/DialerSimulator';
